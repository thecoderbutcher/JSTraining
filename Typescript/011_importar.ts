// Importar en typescript
// Para importar una clase o una interface en TypeScript se debe usar la palabra reservada import
import {Test, numeroExportado} from './010_exportar';

let test = new Test();
console.log(test);
console.log(numeroExportado);
