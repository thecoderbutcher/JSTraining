/* 
Reglas de nomenclatura.
¿Cómo nombro correctamente mis variables, funciones, etc...?

    Nombres pronunciables y expresivos. Preferentemente en inglés usando camelCase🐫, evitando guiones - o _ y abreviaturas.
    Nombres sin información técnica. Evitar nombres con relación a la tecnología (tipo de datos, clases, etc). Ej. "arrayNames" --> "namesList"
    Usar lenguaje ubicuo (ubiqueé???): aquel que se construye a partir del lenguaje que usan los expertos, es decir: crear un lenguaje en común 
para desarrolladores e interesados, determinando palabras de uso común. 
*/
let firsName //good
let arrayNames //bad
let namesList //good

getUserInfo() //bad
getClientInfo() //bad
getUser() //good